<h1>Here's a list of all the registered dentists</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Registration Status</th>
      <th>Registered Since</th>
    </tr>
  </thead>
  <tbody>
    <% @dentists.each do |dentist| %>
    <tr>
      <td> <%= dentist.name %> </td>
      <td><%= dentist.registration_status ? "Registered" : "Not Verfied" %></td>
      <td><%= dentist.registered_since %></td>
    </tr>
    <% end %>
  </tbody>
</table>