<%= form_with(model: dentist) do |form| %>
  <% if dentist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dentist.errors.count, "error") %> prohibited this dentist from being saved:</h2>

      <ul>
        <% dentist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :registration_status, style: "display: block" %>
    <%= form.check_box :registration_status %>
  </div>

  <div>
    <%= form.label :registered_since, style: "display: block" %>
    <%= form.date_field :registered_since %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
